<!-- res/drawable/custtomedit.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Góc bo của hình dạng -->
    <corners android:radius="2dp" />
    <!-- Màu nền của TextInputLayout -->
    <solid android:color="#d1d1e9" />
    <!-- Đường viền TextInputLayout (nếu cần) -->
    <stroke android:width="1dp" android:color="@color/black" />
</shape>
